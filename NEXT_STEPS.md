# NEXT STEPS

## Priority order
1. Create a real Xcode SwiftUI iOS app project for IngrediScore.
2. Add the existing `ingrediscore-native/` source files into the app target.
3. Resolve any target membership, naming, module, asset, or configuration issues.
4. Attempt a simulator build.
5. Fix initial build errors until the app shell launches or the next real blocker is identified.
6. Record blockers and decisions in `BLOCKERS.md`.
7. Record execution notes in `handoffs/mac-to-coordinator.md`.

## Defaults unless changed by Jonathan
- App name: `IngrediScore`
- Interface: SwiftUI
- Language: Swift
- Bundle identifier: `com.jonathan.ingrediscore`
- Use the Mac as the primary iOS build machine

## Coordination rule
Before starting a new major step, check `STATUS.md`, `NEXT_STEPS.md`, and both handoff files.
