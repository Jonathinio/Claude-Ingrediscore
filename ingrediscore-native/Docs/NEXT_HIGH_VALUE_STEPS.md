# Next High-Value Steps

## 1. Real APIClient implementation
Implement HTTP request/response handling, error decoding, timeout behavior, and environment-based base URL configuration.

## 2. AppEnvironment live/mock switching
Allow the app to bootstrap mock repositories now and live repositories later without changing feature code.

## 3. Native scanner implementation on Mac/Xcode
Replace placeholder scan views with:
- AVFoundation barcode scanning
- Vision OCR capture flow

## 4. SwiftData/Core Data replacement for in-memory cache
Persist history and cached products beyond session lifetime.

## 5. Backend implementation against chosen runtime
Stand up the first real API layer behind the contracts already defined.
